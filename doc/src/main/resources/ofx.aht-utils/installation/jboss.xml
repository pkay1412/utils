<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<ofx:section xmlns:ofx="http://www.openfuxml.org" id="how">
	<ofx:title lang="en">JBoss</ofx:title>
	<ofx:paragraph lang="de">
		Der Applikationsserver wird im Verzeichnis \texttt{bin} mit dem Befehl \texttt{standalone.sh}
		(oder \texttt{standalone.bat}) gestartet. Der Startprozess sollte fehlerfrei durchlaufen und
		mit folgender Meldung abgeschlossen werden:
	</ofx:paragraph>
	<ofx:paragraph lang="de">
		Le serveur d'applications est démarré dans le répertoire  \texttt{bin} avec la commande \texttt{standalone.sh} (ou \texttt{standalone.bat}). Le processus de démarrage devrait fonctionner sans accroc et se terminer avec l'indication suivante:
	</ofx:paragraph>
	<ofx:paragraph lang="de">
		Die ZIP-Datei \texttt{lgp-jboss-modules.zip} muss nun im Verzeichnis \texttt{modules} entpackt werden, so dass folgende Struktur entsteht:
	</ofx:paragraph>
	<ofx:paragraph lang="fr">
		Le fichier ZIP \texttt{lgp-jboss-modules.zip} doit être déchargé dans le répertoire \texttt{modules} de telle manière à ce que l'on ait la structure suivante:
	</ofx:paragraph>
		
	<ofx:section>
		<ofx:title lang="en">Linux (Debian Wheezy)</ofx:title>
		
		<ofx:paragraph>
			cd /opt
			tar -xvzf path_to_download/jdk-7u3-linux-x64.tar.gz
			
			update-alternatives --install /usr/bin/java java /opt/jdk1.7.0_21/bin/java 1
			update-alternatives --install /usr/bin/javac javac /opt/jdk1.7.0_21/bin/javac 1
			update-alternatives --set java /opt/jdk1.7.0_21/bin/java
			update-alternatives --set javac /opt/jdk1.7.0_21/bin/javac
		</ofx:paragraph>
	</ofx:section>
</ofx:section>